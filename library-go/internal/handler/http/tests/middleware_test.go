package http

import (
	"github.com/golang/mock/gomock"
	"testing"
)

func TestMiddleware_createArticle(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_updateArticleFile(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_createBook(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_updateBookFile(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_createVideo(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_updateVideoFile(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_createAudio(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_updateAudioFile(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}

func TestMiddleware_sortAndFilters(t *testing.T) {
	testTable := []struct {
		name string
	}{}
	for _, testCase := range testTable {
		t.Run(testCase.name, func(t *testing.T) {
			c := gomock.NewController(t)
			defer c.Finish()
		})
	}
}
